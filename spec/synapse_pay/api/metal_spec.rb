require 'spec_helper'

describe SynapsePay::Api::Metal do
  it 'has a version number' do
    expect(SynapsePay::Api::Metal::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
